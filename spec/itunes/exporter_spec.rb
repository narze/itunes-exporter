require "spec_helper"

RSpec.describe Itunes::Exporter do
  it "has a version number" do
    expect(Itunes::Exporter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
